DATABASE_URL=postgresql+psycopg://flashcards:flashcards@localhost:5432/flashcards

MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=flashcards
MINIO_SECRET_KEY=flashcards123
MINIO_BUCKET=flashcards
MINIO_SECURE=false

SMTP_HOST=localhost
SMTP_PORT=1026
SMTP_FROM=fiszki@local.test
# Mailpit doesn't need auth locally; set these when pointing at a real SMTP provider
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=false
